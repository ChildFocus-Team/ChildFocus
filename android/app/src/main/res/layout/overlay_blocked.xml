<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#CC000000"
    android:padding="24dp">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⚠ Content Blocked"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <!-- OIR Score — R.id.tvScore used in both Kotlin files -->
    <TextView
        android:id="@+id/tvScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OIR Score: —"
        android:textColor="#AAFFFFFF"
        android:textSize="13sp"
        android:layout_marginBottom="24dp"/>

    <!-- Safe suggestions label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Try one of these instead:"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:layout_marginBottom="12dp"/>

    <!-- Loading spinner — R.id.pbSuggestions used in ChildFocusAccessibilityService -->
    <ProgressBar
        android:id="@+id/pbSuggestions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:visibility="visible"/>

    <!-- Suggestion cards container — R.id.llSuggestions used in both Kotlin files -->
    <LinearLayout
        android:id="@+id/llSuggestions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="24dp"/>

    <!-- Dismiss button — R.id.btnClose used in ChildFocusAccessibilityService -->
    <Button
        android:id="@+id/btnClose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dismiss"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF5252"/>

</LinearLayout>